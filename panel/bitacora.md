# Bitácora del panel

Lo único que escribe `/hoy` (`logica-panel.md` §2 y §7). Tres cosas: qué se
decidió cada día, qué decisiones quedan abiertas, y qué no supo decir en la vida
real. **Solo se añade al final de cada tabla; no se edita lo anterior.**

---

## Tabla 1 — días

| Fecha | Panel recomendó | Se hizo | De qué se habló |
|---|---|---|---|
| 2026-08-19 | — | montar el panel | Repaso del sistema entero. Se movió `/ingles` a `estructuras/` y se creó `panel/`. Diagnóstico: el cuello de botella no son más ejercicios, es frecuencia + el input (comprensión). |
| 2026-08-19 | `/ingles` (6 estructuras vencidas) | — (se parcheó el sistema) | Jorge preguntó si la regla examen/refuerzo estaba activa. Estaba escrita en `logica-estructuras.md` §9 y `logica-verbos.md` §11, pero **los comandos no la ejecutaban**: ni detectaban sesión previa del día ni condicionaban la escritura. Se parchearon los dos comandos y `estado.sh`. Las 3 filas infladas del 18-ago son anteriores a la regla: se dejan como están. |
| 2026-08-19 | `/ingles` (6 estructuras vencidas) | — (repaso del ejercicio + 4 bugs y la columna `Esquiva`) | Repaso entero de `estructuras/`. Se decidió **no tocar peldaños ni escalera** (queda pendiente el 🌟, que hoy `cierre.sh` colapsa a ✅ y no aporta nada al SRS). Sí se arreglaron los 4 bugs y se montó la columna `Esquiva`. |
| 2026-08-21 | `/talk` (11 GAPs 🔴 sin recuperar; `/ingles` y `/verbs` ya hechos hoy) | — (se construyó `/drill`) | Primer panel desde el 19: dos días sin anotar. Jorge pidió planear el modo drill. Al mirar la cola salió que **7 de los 9 atascos son verbos**, no estructuras, así que el drill se diseñó para los dos ejercicios y no solo para `/ingles`. Decidido en tres pasos: primero como paso de arranque integrado, luego Jorge pidió **comando propio con ítems saliendo hasta que él pare** — *"un ejercicio rápido justo de lo que estoy fallando para hacer en cualquier momento"*. Su versión es mejor: como comando propio **toca menos ficheros del método** que integrado en `/ingles` y `/verbs`, que quedan intactos. |
| 2026-08-24 | `/ingles` (14 estructuras vencidas) | `/drill` | Tras 1 día sin sesión (racha 0), las dos colas acumuladas: 14 estructuras y 16 verbos vencidos. Señalado que la cola de drill se ha vuelto cola de verbos: 8 de 10 ítems, 6 con rodeo fosilizado. Recordadas las ⏳ (`/listen` sin construir) y que `/talk` supera los 4 días mañana. Jorge lanzó `/drill`. |
| 2026-08-26 | `/ingles` (10 estructuras vencidas, regla 1) | `/verbs` | Primer panel desde el 24. El 25 se hicieron `/ingles` y `/verbs`: las colas de repaso bajaron (estructuras 14 → 10, verbos 16 → 5, ya por debajo del umbral de 8), pero la **cola de drill subió de 10 a 14 ítems**, con tres esquivas nuevas (`claim`, `argue`, `it turns out (that)`) y `run out of` en 4 ⚠️ / 0 ✅. Señalado que 9 de los 14 atascos son verbos con rodeo fosilizado: se practica y la cola crece igual. Recordadas las ⏳ (`/listen` sin construir; «estrenar /chain y /verbs» parece ya cumplida, pendiente de confirmar con él) y que `/talk` lleva 6 días, por encima del límite de 4 de la regla 2. Jorge eligió `/verbs`. |
| 2026-08-26 | `/ingles` (10 estructuras vencidas, regla 1) | `/drill` | Segundo panel del día. Tras `/verbs` la cola de repaso de verbos quedó a 0 y salieron del pozo `claim` y `run out of`, pero la cola de drill subió de 14 a 16 con cuatro esquivas nuevas de golpe (`mind`, `avoid`, `come up with`, `dismiss`): 13 de los 16 atascos son rodeo. Recordadas las ⏳ (`/listen` sin construir; «estrenar /chain y /verbs» pendiente de cerrar) y que `/talk` lleva 6 días. Jorge lanzó `/drill`. |

---

## Tabla 2 — decisiones abiertas

Lo que se decidió construir o cambiar y todavía no está hecho. Se marca con
fecha cuando se ejecuta.

| Fecha | Decisión | Estado |
|---|---|---|
| 2026-08-19 | Mover `/ingles` a su propia carpeta `estructuras/` | ✅ hecho 2026-08-19 |
| 2026-08-19 | Panel diario `/hoy` con snapshot automático | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Modo drill** en `/ingles`: si una estructura acumula ≥2 ❌, la ronda arranca con 4 micro-ítems aislados de esa estructura, con la salida fácil prohibida | ✅ hecho 2026-08-21 — **cambiando de forma**: no es un paso de arranque de `/ingles`, es `/drill`, comando propio (fila de abajo) |
| 2026-08-19 | **`/listen`**: el ejercicio de comprensión que falta. Jorge trae un audio real (podcast político, vídeo OSINT, algo que le dijo su pareja) y se trabaja comprensión + los giros que se le escaparon | ⏳ pendiente |
| 2026-08-19 | Estrenar `/chain` y `/verbs` — banco cargado, 0 sesiones | ⏳ pendiente |
| 2026-08-19 | **Bug corregido:** `/ingles` y `/verbs` no aplicaban examen/refuerzo (§9 / §11). Añadido paso 0 de detección por fecha del log, cierre condicionado (en refuerzo `Veces vista`/`Vistas` no sube y solo se escribe para empeorar) y bloque nuevo en `estado.sh` que dice si la próxima sesión del día es EXAMEN o REFUERZO | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Bug corregido:** la escala tiene 4 notas y la tabla tenía 2 columnas — el `⚠️` no sumaba en ninguna y se evaporaba (subía `Veces vista` y nada más). Añadida columna **`Regular`**, recuperadas del log las 5 filas afectadas, documentada la regla en `logica-estructuras.md` §5 con el invariante `Bien + Regular + Mal = Veces vista`, desplazados los campos `awk` de `estado.sh` (+1 tras `Bien`) y añadidas 2 alarmas: **estancada** (≥3 ⚠️ y 0 ✅) y **tabla descuadrada**. El cierre de `/ingles` verifica el invariante | ✅ hecho 2026-08-19 |
| 2026-08-19 | **`panel/cierre.sh`** — el modelo puntúa, el script calcula. Implementa §5/§7 y §9/§11 tal cual, sin cambiar reglas. Añadida columna **`Últ.`** a los dos progresos (sin ella no se puede deducir el escalón actual: `intervalo = Próxima − Últ.`). Los cierres de `/ingles` y `/verbs` ya no editan la tabla a mano. Probado contra las 11 filas con historial real | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Pendiente decidido a favor de lo conservador:** `cierre.sh` NO cambia el comportamiento del SRS. Los **intervalos conscientes del retraso** (acertar una vencida hace 9 días debería subir dos escalones; fallarla no es error fosilizado sino olvido) quedan para más adelante, encima de una base ya probada | ⏳ pendiente |
| 2026-08-19 | **Mismo agujero en `verbos/`, corregido.** Tenía la misma tabla de 2 columnas para 4 notas. Añadida `Regular` a las 100 filas (0 sesiones = 0 filas que reconstruir, salió gratis), regla e invariante `Bien + Regular + Mal = Vistas` en `logica-verbos.md` §7, campos `awk` de `estado.sh` desplazados (+1 tras `Bien`) y 2 alarmas nuevas: **verbo reincidente** (≥2 ❌, rodeo fosilizado) y **verbo estancado** (≥3 ⚠️ y 0 ✅ — sabe el verbo, nunca lo monta bien). El cierre de `/verbs` verifica el invariante | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Escalera de intervalos 1-3-7-14-30 → 1-2-4-8-10.** El ciclo entero era demasiado lento para un banco que se estrena: una estructura tardaba 55 días en llegar arriba y el repaso llegaba cuando ya se había olvidado. Cambiado en `cierre.sh` (el array `ESC`), `logica-estructuras.md` §5/§8, `logica-verbos.md` §6/§7/§10 y `.claude/commands/verbs.md`. **La escalera vivía DOS veces en `cierre.sh`**: el array y una línea que deducía el peldaño a mano (`(iv<=1)?0 : (iv<=3)?1 : …`) — hardcodeada a la escala vieja y fácil de olvidar. Sustituida por `peldano()`, que la deriva del array: ahora cambiar la escala es cambiar **una** línea. **Migradas las 11 filas con historial** de `progreso-estructuras.md`: sus fechas venían de la escala vieja, así que los huecos 1/3/7 ya no eran peldaños válidos y `cierre.sh` los habría leído mal en silencio (un hueco de 3 días se habría interpretado como peldaño 2, saltándose uno). Remapeadas por peldaño (1d→1d · 3d→2d · 7d→4d) y recalculada `Próxima revisión` desde `Últ.`. Verificado: 11/11 filas dan peldaño válido, 81 filas con el invariante intacto, `estado.sh` idéntico antes y después | ✅ hecho 2026-08-19 |
| 2026-08-19 | **El criterio de ✅ ya no habla de días, habla de peldaños.** Decía "haber alcanzado el intervalo de 14 días" y 14 dejó de existir. Reescrito como **penúltimo peldaño** en `/ingles` (el último es el de mantenimiento, donde ya viven las ✅) y **último peldaño** en `/verbs` (uno más alto justo para garantizar el acierto en N4). Los niveles N1-N4 de `logica-verbos.md` §6 estaban atados a días (N1=1d, N2=3d…) y pasan a ir por peldaño (N1=0, N2=1, N3=2, N4=3-4). **Ojo:** el umbral numérico NO se ha movido (`/ingles` sigue en índice 3, `/verbs` en 4) — subir `/ingles` al último peldaño exigiría 4 aciertos y dejaría muerta la regla "3 aciertos en 3 días distintos", que no se toca. Solo cambia cómo está redactado, para que la próxima escalera no rompa nada | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Ronda de `/ingles` 8 → 10 estructuras.** Con 81 en el banco y 8 por ronda el banco tardaba demasiado en darse una vuelta entera. Cambiado en `logica-estructuras.md` §6/§7/§8/§9, `.claude/commands/ingles.md` y la tabla comparativa de `talk/logica-talk.md` §1. Arrastres: frases 4-6 → **4-5** (10 estructuras a 2-3 por frase), umbral de "solo repaso" 8 → **10 vencidas**, y las filas que se sacan del banco con `grep`, 8 → 10. **Sin tocar** el máximo de 2-3 nuevas por sesión | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Tope de la escalera en 10 días:** con 81 estructuras en ✅ son 8,1 repasos/día sobre una ronda de 10 = 81% de mantenimiento. No muerde hasta tener ~40-50 en ✅ (3-4 meses). Revisar entonces: o se estira el tope a 30-60 días, o se recorta el banco, o el ✅ sale del ciclo | ⏳ pendiente |
| 2026-08-19 | **El 🌟 ya no se pierde: columna `Nativas`.** `cierre.sh` lo colapsaba a `OK`, así que la distinción costaba atención en cada corrección y moría ahí. **Descartada la propuesta de que el 🌟 saltara dos peldaños** — Jorge fue tajante: la escalera se sube **de uno en uno, nunca dos**, porque saltarse un peldaño es programar un repaso a un intervalo que la estructura no ha demostrado aguantar. Así que el 🌟 mueve el intervalo **exactamente igual que un ✅** y lo único que añade es su marca en `Nativas`, subconjunto de `Bien` (`Nativas ≤ Bien`, verificado en cada cierre). Sirve para lo que `Bien` no distingue: aprobar raspando vs. clavarlo. Lo usan el coach al armar la ronda y el panel; **el SRS ni se entera**. Columna al final (no mueve ningún índice de `estado.sh`), recuperados del log los 3 🌟 históricos —`end up + -ing` 1 y `One thing led to another` 2; **no** el de `can't be bothered`, que fue repesca y el día contaba ❌—, listón del 🌟 endurecido en §3 ("si dudas, es ✅") y cobertura del panel ampliada. Probado: una estructura con solo 🌟 y otra con solo ✅ recorren fechas y estados **idénticos** | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Cuatro bugs corregidos tras repasar `estructuras/` entero.** (1) El coach seguía **cantando el nombre en inglés** de la estructura antes de la frase — regalaba el molde. La regla acordada el 18-ago (sesión 3) nunca se aplicó a `logica-estructuras.md` §3/§7/§8 ni a `ingles.md`, que además se contradecía a sí mismo. Ahora se declara **cuántas** y **su función en español**; los nombres solo en la corrección. (2) El snippet awk del invariante estaba **muerto en los dos progresos**: `NF==9` cuando las filas tienen 10 (y `NF==12` cuando tienen 13) — se añadió la columna `Regular` y nadie lo movió, así que no comprobaba nada aunque la tabla estuviera rota. Reescrito **resolviendo por nombre de cabecera**, como `cierre.sh`, para que no vuelva a caducar. (3) `cierre.sh`, refuerzo: descontaba de `Bien` a ciegas, pudiendo **borrar un acierto de otro día** cuando la nota de hoy había sido ⚠️. Ahora deduce la nota de hoy del estado (🔴/✅) y, si la fila está 🟡 (ambigua), exige `FAIL/OK` o `FAIL/REG` y **aborta sin escribir** si no se la pasan. (4) No había guardia contra **dos exámenes el mismo día**: si la detección de §9 fallaba, `Veces vista` subía dos veces y se podía promocionar a ✅ con dos aciertos del mismo día. Ahora aborta si la fila ya tiene `Últ. = hoy`. Los cuatro caminos probados en sandbox | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Columna `Esquiva` + regla anti-salida-fácil.** El patrón de evasión era invisible: Jorge esquivó `can't be bothered` con `didn't feel like` **tres sesiones seguidas** y solo constaba en prosa dentro del log. Esquivar no puntúa como fallo —dice algo correcto— así que la estructura se quedaba en ⚠️ y el sistema no la atacaba nunca. Añadida `Esquiva` como **última** columna de `progreso-estructuras.md` (no mueve ningún índice de `estado.sh`), rellenada con los dos casos que el log ya documenta, alarma nueva **estructura esquivada** en `estado.sh` y en `logica-panel.md` §5, y regla en `logica-estructuras.md` §2: **si tiene salida fácil anotada, la frase ES tiene que dejarla inservible** — no basta con que la estructura quepa, tiene que ser la única que diga lo que la frase pide. La escribe `cierre.sh` con la sintaxis `nombre ~ lo que dijo`, así que **el progreso sigue sin tocarse a mano** — y de paso el `Sustituto` de `/verbs`, que sí se editaba a mano, deja de hacerlo | ✅ hecho 2026-08-19 |
| 2026-08-19 | **Auditoría del núcleo del SRS: 5 bugs más, todos de contabilidad.** Jorge pidió revisar a fondo lo que decide aciertos y fallos. (1) **La peor nota no mandaba.** Si el coach emitía dos líneas de la misma estructura (sale en dos frases, §7), ganaba **la última**, no la peor — justo el caso que §7 prohíbe: falló en la frase 2 y acertó en la 5 tras leer la corrección, y el SRS lo contaba como acierto. Ahora se queda con la peor y lo dice. (2) **`peldano()` redondeaba HACIA ARRIBA.** Un hueco fuera de escala (5 días) se leía como el peldaño de 8: con un acierto saltaba al último peldaño **y se promocionaba a ✅**. Ahora redondea hacia abajo y avisa. Nunca puede regalar peldaño. (3) **Clave duplicada en las dos tablas del progreso** → una sola nota actualizaba **las dos filas**. Ahora aborta. (4) **Un `|` en la salida fácil** partía la fila en dos columnas y rompía la tabla para todos los `awk` posicionales de `estado.sh`. Ahora aborta. (5) `~` a secas dejaba la celda vacía en vez de `—`. Verificado además con una **simulación de ciclo de vida completo** (⚪→2d→4d→⚠️ repite 4d→8d y ✅→❌ baja a 🔴 y resetea) y con la **asimetría examen/refuerzo** de §9 sobre un día entero, incluida la 3ª sesión: `Veces vista` no se movió y el invariante aguantó en todas las escrituras reales | ✅ hecho 2026-08-19 |
| 2026-08-21 | **`/drill` — quinto ejercicio, el de bolsillo.** Micro-ítems de lo atascado saliendo en **bloques de 4 del mismo ítem** hasta que Jorge dice `para`. Sin ronda, sin duración, y **cortarlo a mitad no cuesta nada** porque no hay estado que cuadrar al salir — es el único que se puede abrir tres minutos en cualquier sitio. **Dos formatos, no uno**, porque las alarmas de `logica-panel.md` §5 no describen el mismo problema: **bloqueo** (rodeo/esquiva — el ítem no se nombra, el estímulo inutiliza la salida fácil por construcción) y **patrón** (estancado — aquí el ítem **sí se da**: 3 ⚠️ prueban que lo recupera bien y lo que falla es el montaje, así que forzarlo cuatro veces más no arregla nada). Metidos en un molde único habría sido el error de diseño del ejercicio. **No puntúa y no llama a `cierre.sh`** — único ejercicio que no lo hace: cuatro aciertos seguidos del mismo ítem, aislado y sabiendo que toca, son recencia, y si puntuaran promocionarían a ✅ por empollar (`logica-estructuras.md` §4.2). Consecuencia que el cierre recuerda **siempre**: el drill entrena, pero **la cola solo se vacía acertando el ítem encadenado en `/ingles` o `/verbs`**. Bloques de 4 y no rotación: masificar va contra el interleaving de §4.3 **a propósito**, porque el objetivo no es aprender el ítem —ya se sabe— sino romper un hábito de evasión, y eso no se rompe con una exposición suelta cada tres días; la segunda vuelta con contextos nuevos es lo que impide que el acierto sea del bloque. Rescate a 0/4: se da el molde y van 2 más, nunca se insiste más allá. Nuevos `drill/logica-drill.md`, `drill/log-drill.md` y `.claude/commands/drill.md`; **intactos los cuatro ejercicios, los cuatro progresos, los bancos y `cierre.sh`** | ✅ hecho 2026-08-21 |
| 2026-08-21 | **Bug corregido: la alarma de drill no se apagaba nunca.** `estado.sh` marcaba drill con `mal >= 2`, y **`Mal` es acumulativo: nunca baja**. Un ítem que fallara dos veces se quedaba listado **para siempre** aunque después acertara cinco — la lista solo podía crecer y en dos semanas habría dejado de servir para elegir. No se había notado porque ningún ítem ha salido todavía del pozo. Condición nueva: **`mal >= 2 && bien <= mal`** — sale cuando los aciertos **superan** a los fallos; empatar no basta. Probado en sandbox: `afford` con 3 ✅ / 2 ❌ desaparece de la cola, con 2 ✅ / 2 ❌ sigue. Las otras dos alarmas ya se apagaban solas (la de estancado muere al primer ✅, la de esquiva al limpiar con `~ —`). Documentado en `logica-panel.md` §5 y `logica-drill.md` §3 | ✅ hecho 2026-08-21 |
| 2026-08-21 | **Bloque `COLA DE DRILL` en `estado.sh`.** Las mismas alarmas de siempre, pero **ordenadas por prioridad** —1 fallos + salida fácil · 2 fallos · 3 estancado · 4 esquiva joven— y con el tipo de drill de cada ítem. Es lo que consume `/drill`, filtrándolo con `sed` en vez de leer los progresos, así que **no hay lógica de cola duplicada**. Añadida también la línea de `/drill` en «última sesión», **fuera de las prioridades de §4 y fuera de la racha**: no puntúa ni hace avanzar ningún banco, y si compitiera el panel acabaría recomendando drill cada día que no se hubiera drilleado. Verificado que el resto del snapshot no cambia ni una línea | ✅ hecho 2026-08-21 |
| 2026-08-19 | **Paridad con `/verbs` comprobada, y dos huecos cerrados.** De los 9 bugs de hoy, **7 viven en `cierre.sh`**, que es compartido, así que `/verbs` los tenía arreglados desde el primer momento (descuento ciego en refuerzo · doble examen el mismo día · peor nota en duplicados · redondeo del peldaño hacia arriba · clave duplicada · `|` en la columna de diagnóstico —allí `Sustituto`— · `~` vacío). El del **awk muerto** estaba duplicado en su fichero (`NF==12` con filas de 13) y se arregló a la vez. El de **cantar la respuesta antes del intento no aplica**: `/verbs` ya decía *No digas cuáles son*. Huecos que sí quedaban: (1) `estado.sh` comprobaba verbos con `NF==13` — el mismo patrón frágil que causó el bug del awk muerto, que funciona hasta que alguien añade una columna; pasado a `NF>=13`. (2) El 🌟 seguía perdiéndose en verbos: sin columna `Nativas`, `cierre.sh` avisaba y lo contaba como ✅. Añadida ahora que **cuesta cero** (0 sesiones = 0 filas que reconstruir), igual que se hizo con `Regular`. De paso, la tabla de métricas de `progreso-verbos.md` tenía un separador de 11 columnas para una cabecera de 9 (descuadre previo, cosmético): igualado | ✅ hecho 2026-08-19 |

---

## Tabla 3 — captura

Lo que Jorge quiso decir en la vida real y no le salió. Se vuelca aquí en
español, tal cual, sin pulir. El coach lo reparte al banco que le toque
(`estructuras/`, `verbos/` o el inventario de GAPs de `talk/`) y entonces marca
la fila como repartida.

| Fecha | Quería decir (ES) | Contexto | Repartido a |
|---|---|---|---|

*(vacía — se llena hablando, no de golpe)*
| 2026-09-04 | — (decisión de Jorge, sin aplicar) | **Reescribir la lógica de `/verbs`.** Jorge, textual: *"el ejercicio ya me tiraba el rodeo y tenía que adivinar el verbo, y un fallo lo manda al drill"*. Tres piezas, no dos: (1) el `ES` no puede sustituir el verbo español por una frase que significa otra cosa — poner «me quejé» para `complain` **no es una pista**, la pista real es un cognado; (2) `log-verbos.md` tiene que **guardar el estímulo** junto a la respuesta, o un enunciado roto se archiva como fallo suyo sin rastro para auditarlo; (3) **el circuito de realimentación**: ese falso fallo entra en la cola de drill como «rodeo fosilizado», así que el ejercicio se alimentaba de sus propios enunciados malos — 9 de los 13 atascos actuales son verbos con rodeo y no se puede saber cuáles son reales. Él lo arregla más adelante (*"luego lo arreglo"*). **Nada tocado:** `logica-verbos.md` sigue intacto | ⏳ pendiente |
